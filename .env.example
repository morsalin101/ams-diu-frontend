# Copy to ".env" on the VPS:  cp .env.example .env
# Used only for routing/SSL by the shared nginx-proxy. The API URL is
# already hardcoded to https://api.tatomal.me in src/services/api.js.
ADMIN_DOMAIN=admin.tatomal.me
LETSENCRYPT_EMAIL=admin@tatomal.me
